const Button = () => {
  return (
    <button
      style={{
        border: "none",
        color: "#fff",
        backgroundColor: "red",
        padding: "3px 15px",
        borderRadius: "5px",
        alignContent: "center",
      }}
    >
      예매하기
    </button>
  );
};

export default Button;
